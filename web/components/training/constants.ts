/**
 * Status colors for training run statuses.
 */
export const STATUS_COLORS: Record<string, string> = {
  queued: 'yellow',
  running: 'blue',
  succeeded: 'green',
  failed: 'red',
  canceled: 'gray',
}
