{
  "title": "LudwigTrainingConfiguration",
  "type": "object",
  "required": [
    "runId",
    "datasetVersionId"
  ],
  "properties": {
    "runId": {
      "format": "uuid",
      "description": "The run ID",
      "type": "string"
    },
    "datasetVersionId": {
      "format": "uuid",
      "description": "The dataset version ID",
      "type": "string"
    }
  }
}