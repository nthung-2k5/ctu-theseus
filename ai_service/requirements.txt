# Theseus AI Service - Python Dependencies

# Health endpoint (optional, can be removed if not needed)
fastapi>=0.136.3
uvicorn[standard]>=0.48.0

# NATS messaging (replaces Celery + Redis + requests/webhooks)
nats-py>=2.10.0

# Deep learning (Ludwig handles torch, torchvision, timm internally)
ludwig>=0.17.5
pandas>=2.3.0

# TFLite conversion (optional)
# tensorflow>=2.21.0
# litert-torch>=0.9.1
